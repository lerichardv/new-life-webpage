[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Optional: Configuration for Netlify's Next.js Runtime
[[plugins]]
  package = "@netlify/plugin-nextjs"
